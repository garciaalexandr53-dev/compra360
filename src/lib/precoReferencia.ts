/** Parâmetros do aviso de preço no portal público do fornecedor. */

/** Divergência tolerada em relação à mediana de referência (20%). */
export const LIMIAR_DIVERGENCIA_MERCADO = 0.20;

/** Faixa fixa usada quando não há referência de mercado. */
export const PRECO_FALLBACK_MIN = 1;
export const PRECO_FALLBACK_MAX = 999;
